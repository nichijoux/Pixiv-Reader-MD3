<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络状态监听（NetworkMonitor 注册 DefaultNetworkCallback / 查 NetworkCapabilities） -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <!-- Android 13+ 后台下载完成/失败的系统通知 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 会话 token（access/refresh）明文存 MMKV：禁止云备份/ADB 备份，防止 refresh_token 随备份外泄 -->
    <!-- Android 16 起 targetSdk 36+ 默认开启预测性返回：手势中先播「页面缩小」预览、
         松手后又播 app 自己的滑动转场，两者拼接观感割裂（缩小然后消失）。
         显式关闭后返回手势直接提交，统一走应用内既定转场（220ms slide + fade） -->
    <application
        android:name=".PixivApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixivReader">

        <!-- PixivApp 实现 Configuration.Provider（HiltWorkerFactory），
             移除 work-runtime 默认的 WorkManagerInitializer（on-demand 初始化） -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <!-- 导出文件分享/系统打开：filesDir/Downloads/novels 下的小说文件 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- OAuth PKCE 登录回调：pixiv://account/login?code=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pixiv" android:host="account" android:pathPrefix="/login" />
            </intent-filter>
            <!-- 作品深链：pixiv://illust/{id} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pixiv" android:host="illust" />
            </intent-filter>
        </activity>
    </application>

</manifest>
