# ── Gradle / Android 构建产物 ────────────────────────────────
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
*.apk
*.aab
*.aar
*.hprof

# pixiv-api-kotlin 上游源码副本（本地参考，不纳入 Git）
pixiv-api-kotlin/

# ── Kotlin / IDE ─────────────────────────────────────────────
.kotlin/
.idea/
*.iml
.vscode/
.DS_Store

# ── 日志 ─────────────────────────────────────────────────────
logs/
*.log

# ── 签名 / 密钥 ──────────────────────────────────────────────
*.jks
*.keystore
keystore.properties
release.jks.b64

# ── 其它 ─────────────────────────────────────────────────────
# 操作系统文件
Thumbs.db
Desktop.ini

# 本地覆盖配置（含本机 SDK/JDK 路径，不应提交）
*.local.properties

# 本机专用 / 工具链配置与设计草稿（仅本地保留，不入库）
.omp/
build-release.bat
build-release.ps1
design/
DESIGN.md
.codegraph
dist/
.zcode/